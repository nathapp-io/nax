# Loaded automatically by Bun when running `bun test` (NODE_ENV=test).
# Prevents real agent spawning (claude, acpx, etc.) which costs money.
# To intentionally run real-agent tests: use `bun run test:real`
CI=1
NAX_SKIP_PRECHECK=1
