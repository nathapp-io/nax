# v0.49.6 — Test Reliability & Webhook Fix

**Released:** 2026-03-19

## Bug Fixes

### Test Suite: Eliminated 38 cross-file test failures

**Root cause:** `mock.module()` in Bun permanently replaces the ESM module registry for the entire process — `mock.restore()` does NOT undo it. Two unit test files used `mock.module()` on `isolation.ts`, `cleanup.ts`, and `git.ts`, poisoning `_isolationDeps`/`_cleanupDeps` for all subsequent files in the same run.

**Fix:** Replaced all `mock.module()` usage with injectable `_deps` objects. Added `_rectificationGateDeps` to `src/tdd/rectification-gate.ts` and extended `_sessionRunnerDeps` in `src/tdd/session-runner.ts` with all external dependencies. Rewrote 8 test files to swap deps via `beforeEach`/`afterEach` instead of touching globals.

Affected: `session-runner-keep-open.test.ts`, `rectification-gate-session.test.ts`, and 6 TDD orchestrator integration tests.

### Webhook `receive()`: Event-driven delivery replaces polling loop

**Root cause:** `receive()` used an exponential-backoff polling loop (100→200→400→800→1600ms). In slow Docker/VM environments the cumulative sleep intervals consumed most of the timeout window before the valid POST arrived, causing the Promise to resolve with `"skip"` (timeout) even though the server correctly returned 200.

**Fix:** Replaced polling with a `receiveCallbacks` Map. `handleRequest()` now resolves the waiting Promise immediately when a valid POST arrives. Timeout handled by a single `setTimeout`. Test time: 4218ms → 65ms.

### Streams: `getChangedFiles` / `getPgid` stdout read fix

Fixed `Bun.readableStreamToText()` usage in `getChangedFiles` and `getPgid` — stdout must be read concurrently with `proc.exited`, not sequentially, to avoid deadlock on large output buffers (>64KB).

### Circular import: `prompts-tdd` ↔ `prompts-main`

Broke circular import between `prompts-tdd.ts` and `prompts-main.ts` by extracting shared types into `prompts-shared.ts`.

### Session options: agent adapter standardization

Standardized session options across all agent adapters — `acpSessionName`, `keepSessionOpen`, and permission flags now consistently applied per-adapter.

### Per-package config: `review.commands` bridge (PKG-006)

`quality.commands` from per-package config now correctly bridges into `review.commands` during merge, fixing cases where package-level test commands were ignored during the review stage.

## Documentation

- `docs/architecture/ARCHITECTURE.md` — added ⚠️ warning: `Bun.spawn` global mutation and `mock.module()` are forbidden; full `_deps` table for all TDD modules
- `.claude/rules/02-test-architecture.md` — new "Process/Spawn Mocking Architecture" section
- `.claude/rules/03-test-writing.md` — "Never mutate `Bun.spawn` globally" rule with injectable deps reference table
