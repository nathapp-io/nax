/**
 * @deprecated — US-003 retired the hardcoded rate table. The MODEL_PRICING,
 * COST_RATES and RATE_CARD_REVIEWED exports are gone; the rate-card resolver
 * in ./rate-card.ts is the only path.
 *
 * This file is intentionally empty so any stale import compiles without
 * behaviour. Delete this stub once downstream code no longer references the
 * path.
 */
export {};
