version: "3.9"
services:
  app:
    image: nathapp/bun:1.3.8-ci
    working_dir: /app
    volumes:
      - .:/app
    command: >
      sh -c "
        bun install &&
        bun run test:unit
      "    
    environment:
      - NAX_SKIP_PRECHECK=1
      - CI=true
