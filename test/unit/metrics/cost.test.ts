/**
 * US-003: this file's surface was `estimateCost`, `estimateCostByDuration`
 * and `COST_RATES`. All three were deleted with the hardcoded rate table.
 * The surviving `formatCostWithConfidence` coverage moved to
 * `test/unit/agents/cost/calculate.test.ts`. The file is retained (rather
 * than removed) only because the sandboxed tooling cannot delete files —
 * its content is intentionally empty.
 */
