/**
 * US-003: this file's entire surface was `estimateCostFromTokenUsage`. That
 * function was deleted with the hardcoded rate table; its per-model rate
 * cases are superseded by the rate-card suite US-001 creates under
 * `test/unit/agents/cost/`. The file is retained (rather than removed)
 * only because the sandboxed tooling cannot delete files — its content is
 * intentionally empty.
 */
