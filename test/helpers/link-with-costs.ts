/**
 * Stub helper for tests that need a cost-linked result shape.
 * Placeholder for future cost-attribution testing utilities.
 */
export function makeLinkWithCosts<T>(value: T, _costUsd = 0): T {
  return value;
}
