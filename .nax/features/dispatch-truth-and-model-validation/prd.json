{
  "project": "@nathapp/nax",
  "feature": "dispatch-truth-and-model-validation",
  "branchName": "feat/dispatch-truth-and-model-validation",
  "createdAt": "2026-10-01T00:00:00.000Z",
  "updatedAt": "2026-09-11T07:07:12.008Z",
  "userStories": [
    {
      "id": "US-001",
      "title": "callOp raises zero-dispatch as its own terminal outcome",
      "status": "in_progress",
      "passes": false,
      "routing": {
        "complexity": "complex",
        "testStrategy": "three-session-tdd-lite",
        "reasoning": "Required outcome-field propagation through all fallback return paths and a new high-impact call error boundary require isolated behavior and regression verification."
      },
      "contextFiles": [
        "src/agents/manager-types.ts",
        "src/agents/manager-run-fallback.ts",
        "src/operations/call.ts",
        "src/operations/call-resolvers.ts",
        "test/unit/operations/call-empty-output.test.ts"
      ],
      "modifiedFiles": [
        {
          "path": "test/unit/operations/call-empty-output.test.ts",
          "reason": "its fixtures build run outcomes without `dispatchesCompleted`, which is now required on `AgentRunOutcome`. Each fixture must set the count that matches what it simulates: an empty-or-whitespace turn that was actually returned by an adapter completed a dispatch, so these fixtures set `dispatchesCompleted: 1` and keep asserting `CALL_OP_NO_OUTPUT`. The invariant that replaces the bare fixture is that `CALL_OP_NO_OUTPUT` covers a completed dispatch with unusable output, and `CALL_OP_NO_DISPATCH` covers no completed dispatch at all."
        },
        {
          "path": "test/unit/agents/manager-types-phase5.test.ts",
          "reason": "it builds an `AgentRunOutcome` object literal with only `result`, `fallbacks`, `finalBundle` and `finalPrompt`, which stops compiling the moment `dispatchesCompleted` becomes required. The invariant that replaces it is that an `AgentRunOutcome` literal carries a `dispatchesCompleted` count alongside `finalBundle` and `finalPrompt`."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Neither review gate produces a verdict from a dispatch that never happened",
      "status": "in_progress",
      "passes": false,
      "routing": {
        "complexity": "complex",
        "testStrategy": "three-session-tdd-lite",
        "reasoning": "Two review gates collapse the no-dispatch state into fail-open / parse-error shapes; surfacing `noDispatch` requires reading `CALL_OP_NO_DISPATCH` at the review phase and wiring a new `CheckResult.noDispatch` field, with three consumers."
      },
      "contextFiles": [
        "src/review/types.ts",
        "src/execution/story-orchestrator/review-decision.ts",
        "src/operations/semantic-review.ts",
        "src/operations/adversarial-review.ts",
        "src/execution/post-run-review-summary.ts"
      ],
      "modifiedFiles": [
        {
          "path": "test/unit/operations/semantic-review.test.ts",
          "reason": "its empty-dispatch case asserts the fail-open shape that this story replaces for the zero-dispatch path. The invariant that replaces it is that an empty output from a *completed* dispatch still fails open, while no completed dispatch yields `noDispatch: true` with `success: false`."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "A rectification pass with zero dispatches skips validation",
      "status": "in_progress",
      "passes": false,
      "routing": {
        "complexity": "complex",
        "testStrategy": "three-session-tdd-lite",
        "reasoning": "Zero-dispatch needs to bypass the existing skip-validate exit while stopping the no-progress counter from charging the iteration — touches the fix-cycle state machine and the rectification phase accounting."
      },
      "contextFiles": [
        "src/findings/cycle.ts",
        "src/findings/cycle-dispatch.ts",
        "src/findings/cycle-types.ts",
        "src/execution/story-orchestrator/rectification.ts",
        "src/execution/story-orchestrator-logging.ts"
      ]
    },
    {
      "id": "US-004",
      "title": "Precheck resolves every configured model id and reports dropped overrides",
      "status": "in_progress",
      "passes": false,
      "routing": {
        "complexity": "complex",
        "testStrategy": "three-session-tdd-lite",
        "reasoning": "A new catalog resolver and precheck check need to thread through every config site that names a model id; one walk covers tier names, literal pins, fallback rungs, and escalation order."
      },
      "contextFiles": [
        "src/precheck/index.ts",
        "src/precheck/types.ts",
        "src/config/selectors.ts",
        "src/config/schema-types.ts",
        "src/agents/native/client.ts"
      ],
      "outOfScope": [
        "network probing of model availability (local catalog lookup only); validating ids for agents other than native and acp."
      ],
      "modifiedFiles": [
        {
          "path": "test/unit/precheck/precheck-checks-tier1-blockers.test.ts",
          "reason": "it pins the tier-1 blocker set by its exact membership, which this story extends with the model-resolution check. The invariant that replaces the fixed membership is that the model-resolution blocker is present in the tier-1 set and runs after `checkAgentCLI`."
        }
      ]
    }
  ],
  "acceptanceCriteria": {},
  "createdBy": "prd-spec",
  "schemaVersion": "1.0"
}
