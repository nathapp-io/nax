describe()` and `test()` from `bun:test`
- File I/O checks for ACs requiring source code or file existence verification

Each test directly validates the acceptance criterion it covers, ensuring the feature works correctly when implemented.